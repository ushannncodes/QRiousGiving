# Working style

<!-- This is the Pi's user-global Claude Code config. After pulling, install it:
       mkdir -p /home/pi/.claude && cp pi_global_CLAUDE.md /home/pi/.claude/CLAUDE.md
     It applies to every project on the Pi, not just QRiousGiving. -->

- Hobbyist/maker doing solo personal projects (hardware + software) — no team,
  no CI/CD pipeline, no external style guide to match. Optimize for shipping
  and learning, not enterprise process.
- Flag incidental issues you notice outside the current task (stray configs,
  messy folders, dead code, unrelated bugs) in one line and move on — don't fix
  them or spawn background tasks unless asked.
